T-001
T-001 VERIFY: pass — scaffold backfill committed in 703f691; project.yml + vmux.xcodeproj generated, builds in Xcode/visionOS sim.
T-002
T-002 VERIFY: pass — Package.resolved pins Citadel + SwiftTerm; vmux/SwiftPMImportCheck.swift references SSHClient + Terminal to prove both packages link.
T-003
T-003 VERIFY: pass — `plutil -lint vmux/Info.plist` returned OK; `NSMicrophoneUsageDescription` present with the PRD-specified copy; `NSSpeechRecognitionUsageDescription` correctly absent (Speech framework not used; Gemini Live replaces it).
T-004
T-004 VERIFY: pass — vmux/Models/{Project,Tab,AppSettings}.swift implement the §4.2 schemas; vmuxApp.swift wires ModelContainer + AppSettings.bootstrap; vmuxTests/ModelsTests.swift covers round-trip, cascade delete, and single-row bootstrap.
T-005
T-005 VERIFY: pass — vmuxApp.swift defines all four scenes (sidebar/settings/terminal-per-UUID/environment-immersive-mixed); vmux/Views/{Sidebar,Settings,TerminalWindow,Skydome}View.swift created; SidebarView wires the three temp buttons (Open Settings / Open Terminal / Toggle Environment) via openWindow + openImmersiveSpace per T-005 Do block; `xcodebuild -scheme vmux -destination 'platform=visionOS Simulator,name=Apple Vision Pro' build` and `test` both pass.
T-006
T-006 VERIFY: pass — vmux/Services/KeychainService.swift implements save/load/delete against kSecClassGenericPassword with service "vmux" and account = ref (upsert via SecItemUpdate→Add fallback, idempotent delete); vmuxTests/KeychainServiceTests.swift covers round-trip, overwrite, delete, missing-load-nil, idempotent-delete, and Unicode; `xcodebuild test -scheme vmux -destination 'platform=visionOS Simulator,name=Apple Vision Pro'` passes (10/10 tests).
T-007
T-007 VERIFY: pass — vmux/Views/SidebarView.swift now uses @Query for projects + tabs, single List with "Projects" and "Tabs in <project>" sections, ⚙ Settings button bottom-left wired via openWindow(id:"settings"), tab rows open terminal windows via openWindow(id:"terminal", value: tab.id), context menus for delete project/close tab via modelContext, no-op handlers for "+ New Project"/"+ New Tab" (deferred to T-008/T-012), and the three T-005 temp buttons are gone; frame is 340×640; `xcodebuild build` and `xcodebuild test` both succeed (10/10 tests).
T-008
T-008 VERIFY: pass — vmux/Views/Components/NewProjectSheet.swift added (name/host/port/username fields, password|privateKey auth picker with SecureField or monospaced TextEditor, Save disabled until name+host non-empty, port ∈ 1…65535, and secret non-empty), wired into SidebarView via .sheet on the "+ New Project" button; on save it generates a UUID keychainRef, persists the secret via KeychainService (stored under kSecAttrService "vmux"/account=keychainRef), inserts a Project, and rolls the secret back if SwiftData save fails; xcodegen regenerated the project, `xcodebuild build` and `xcodebuild test` both succeed (10/10 tests).
T-009
T-009 VERIFY: pass — vmux/Views/SettingsView.swift now renders a Form with Profile, OpenAI key, Gemini key + model picker (gemini-2.5-flash default, gemini-2.5-pro, or custom string bound to AppSettings.geminiModel), and idle-threshold slider (1…10, integer-snapped via Double binding) bound to AppSettings via @Bindable; keys are loaded from Keychain on appear and persisted on .onChange (generating UUID keychainRefs once and reusing, deleting from Keychain when cleared); Test OpenAI does GET https://api.openai.com/v1/models with Bearer auth → ✓/✗, Test Gemini does GET https://generativelanguage.googleapis.com/v1beta/models?key=<key> → ✓/✗, both surfaced via a statusBadge (idle/progress/green check/red xmark); `xcodebuild build` and `xcodebuild test` both pass (10/10 tests).
T-010
T-010 VERIFY: pass — vmux/Services/SSHConnectionManager.swift adds an `actor SSHConnectionManager` keyed by Project.id with `client(for: SSHProjectInfo)` (Sendable struct extracted from Project on the MainActor), `disconnect(projectID:)`, and `disconnectAll()`; lazy connect via Citadel's `SSHClient.connect(host:port:authMethod:hostKeyValidator:.acceptAnything(),reconnect:.never,...)`, dead-client eviction on `onDisconnect` so the next `client(for:)` reconnects; auth method factory is nonisolated static and supports password + OpenSSH-format ed25519 / RSA private keys (detected via SSHKeyDetection, throws SSHConnectionError.{missingSecret, unsupportedAuthType, unsupportedPrivateKeyType, invalidPrivateKey}); retroactive @unchecked Sendable conformances on SSHClient + SSHAuthenticationMethod to satisfy Swift 6 strict concurrency; vmuxTests/SSHConnectionManagerTests.swift covers password-auth factory, unknown-auth-type throw, garbled-private-key throw, missing-secret throw on `client(for:)`, plus a live `echo ok` integration test gated on VMUX_TEST_HOST/USER/PASSWORD|KEY|KEY_PATH (XCTSkip otherwise); `xcodebuild build` and `xcodebuild test` both succeed (14 passed, 1 live test correctly skipped).
T-011
T-011 VERIFY: pass — vmux/Services/TerminalSession.swift adds a `TerminalShellChannel` protocol (Sendable, vends an `AsyncStream<Data> inbound`, async `send(_:)` and `close()`) plus a @MainActor @Observable `TerminalSession` that owns a SwiftTerm `Terminal` (80×24, 500-line scrollback) wired through a forwarding `TerminalSessionDelegate`; init starts an inbound pump Task that feeds each chunk into the terminal and updates `lastByteAt`, the delegate routes terminal-generated bytes back to `channel.send(...)`, and the public `send(_:)` exposes the same path for the future SpeechCoordinator; `close()` cancels the pump and closes the channel. vmuxTests/TerminalSessionTests.swift adds three @MainActor tests via a Sendable `StubTerminalShellChannel` (AsyncStream-backed inbound, actor-recorded writes): feeding "Hello, vmux!" into the stub lands on row 0 of the terminal buffer (verified via Terminal.getCharacter) and bumps `lastByteAt`; calling `session.send("pwd\n")` records exactly that payload in the stub; and `close()` halts the pump so post-close yields are ignored. `xcodebuild build` and `xcodebuild test` both succeed (17 passed, 1 live SSH test correctly skipped).
T-012
T-012 VERIFY: pass — vmux/SwiftTerm+SwiftUI.swift (SwiftTermView: UIViewRepresentable) embeds session-owned TerminalView; TerminalSession refactored to own a SwiftTerm.TerminalView (UIKit) instead of a bare Terminal, with TerminalViewDelegateAdapter forwarding keystrokes back to channel.send and the inbound pump feeding terminalView.feed(byteArray:); CitadelShellChannel wraps client.withPTY (xterm-256color, 80x24) — actor-style state via NIOLockedValueBox, spawns a pump task, awaits an external close signal inside the perform closure, signals close on EOF; TerminalSessionRegistry (@MainActor singleton) caches one session per Tab.id, deduplicates inflight connects, and exposes remove(tabID:) for sidebar deletion; TerminalWindowView fetches Tab via FetchDescriptor<vmux.Tab>, shows title + project subhead, loads session via .task(id: tabID), surfaces connection errors with Retry, and renders SwiftTermView; SidebarView wires "+ New Tab" → insert Tab + openWindow(id:"terminal", value: tab.id), close-tab → modelContext.delete + dismissWindow + registry.remove, delete-project → cleanup of all child tabs' sessions; closing the window does NOT call registry.remove, so reopening continues from the same session. xcodebuild build + xcodebuild test both pass (17 passed, 1 live SSH test correctly skipped).
T-013
T-013 VERIFY: pass — vmux/Services/ActivityMonitor.swift adds an @MainActor monitor that polls TerminalSession.lastByteAt on a 500ms RunLoop.common timer; on byte-arrival it sets tab.isRunning=true + tab.lastActivityAt=lastByteAt and saves, and once Date.now-lastByteAt > AppSettings.idleThresholdSeconds (re-read each tick) it flips tab.isRunning=false, saves, and fires AudioServicesPlaySystemSound(1004) (onDone closure injected for testability). TerminalSessionRegistry now also holds a monitors[Tab.id: ActivityMonitor] dictionary, starts a monitor when a session is created (using tab.modelContext for both Tab updates and AppSettings lookup), and stops it in remove(tabID:). vmuxTests/ActivityMonitorTests.swift covers deterministic active→idle transition with injected nowProvider+onDone, second-cycle re-fire after re-activation, and an end-to-end timer test that observes isRunning bounce true→false. xcodebuild build + xcodebuild test both pass (21 tests, 1 live SSH integration skipped).
T-014
T-014 VERIFY: pass — vmux/Views/Components/TabStatusDot.swift adds a pure TabStatusState (running/justFinished/idle) derived from (isRunning, lastActivityAt, now) per §T-014 spec, plus a TabStatusDot SwiftUI view that wraps a TimelineView(.periodic by 0.5s) around an Image(systemName: "circle.fill") tinted .orange/.green/.gray; the .justFinished state pulses via .symbolEffect(.pulse, isActive: true). SidebarView's tab row now uses TabStatusDot(isRunning:, lastActivityAt:) instead of the placeholder gray Circle. vmuxTests/TabStatusDotTests.swift covers all five cases: isRunning→running, isRunning takes precedence even when lastActivityAt is ancient, idle <5s→justFinished, idle >5s→idle, and the exact 5s boundary stays justFinished (matching the spec's strict > 5s gray rule). xcodebuild build + xcodebuild test both pass (26 tests, 1 live SSH integration correctly skipped).
T-015
T-015 VERIFY: pass — vmux/Services/FocusStore.swift adds @MainActor @Observable FocusStore (singleton via `static let shared`, var focusedTabID: UUID?); TerminalWindowView captures `private let focusStore = FocusStore.shared`, attaches `.onHover { hovering in if hovering { focusStore.focusedTabID = tabID } }` (sticky — hover-out is a no-op so focus only moves when another tab is hovered), and overlays a `RoundedRectangle.strokeBorder(Color.blue.opacity(0.55), lineWidth: 3)` with blue shadow when `focusStore.focusedTabID == tabID` (allowsHitTesting false, accessibilityHidden true so it does not steal gaze or affect VoiceOver). vmuxTests/FocusStoreTests.swift covers initial-nil, set/overwrite, sticky hover-out semantics via an applyHover helper, and singleton identity. `xcodebuild build` and `xcodebuild test` both pass (22 passed + 1 live SSH skipped, 5 new FocusStore tests included).
T-016a
T-016a VERIFY: pass — vmux/Services/GeminiLiveClient.swift adds `actor GeminiLiveSession` with nonisolated `events: AsyncStream<TranscriptEvent>`, `start()`/`sendAudio(_:)`/`close()` methods, a `GeminiWebSocketChannel` protocol injectable via `channelFactory` (production wrapper `URLSessionGeminiChannel` wraps `URLSessionWebSocketTask`); `start()` opens `wss://generativelanguage.googleapis.com/ws/google.ai.generativelanguage.v1beta.GenerativeService.BidiGenerateContent?key=<KEY>`, sends the §4.4 setup JSON, spawns a receive loop that parses `setupComplete` + `serverContent.inputTranscription.text`, exponential-backoff reconnect (250 ms → 4 s, capped at 5). 5 new tests via StubGeminiChannel. xcodebuild build + test both pass (28 executed, 1 live SSH skipped).
T-016b
T-016b VERIFY: pass — vmux/Services/AudioFormatConverter.swift wraps one AVAudioConverter (PCM16 mono 16 kHz); vmux/Services/SpeechCoordinator.swift adds @MainActor @Observable singleton owning one AVAudioEngine + one GeminiLiveSession at a time, observes FocusStore.focusedTabID, opens/tears down session on focus change, appends incremental transcript fragments. SidebarView .task bootstraps credentialsProvider (SwiftData + Keychain) and calls SpeechCoordinator.shared.start(). 11 new tests. xcodebuild build + test pass (39 executed, 1 live SSH skipped).
T-017
T-017 VERIFY: pass — vmux/Views/Components/TranscriptPill.swift adds a top-center translucent Capsule overlay (🎙 emoji + quoted text) bound to SpeechCoordinator.partialTranscript; 2 s hide timer cancelled+rescheduled on each fragment; TerminalWindowView wires SpeechCoordinator.shared and renders the pill inside .overlay(alignment: .top) only when focusStore.focusedTabID == tabID. xcodebuild build + test pass (39 executed, 1 live SSH skipped).
T-018
T-018 VERIFY: pass — vmux/Services/SpeechCoordinator.swift adds commit logic: trigger words [" send", " enter"] (case-insensitive, leading-space required), 1.0 s silence interval, commitDelivery callback routes UTF-8(text+"\r") to FocusStore.shared.focusedTabID (re-read AT COMMIT TIME, not at speech-start). The Gemini Live WebSocket is NOT closed between commits — only the buffer resets. 9 new tests cover both triggers, case-insensitivity, silence timer reset, consecutive commits keeping one WS open, focus-at-commit-time race, mid-utterance focus-change drop, and whitespace-only no-op. xcodebuild build + test pass (48 executed, 1 live SSH skipped).
T-024
T-024 VERIFY: pass — vmux/Services/TerminalSession.swift adds `enum SessionStatus { connecting, connected, disconnected(reason:) }` (Equatable, Sendable) and an observable `status` property; transitions are: `.connecting → .connected` on first inbound byte, → `.disconnected` when the pump's inbound stream ends (channel EOF). vmux/Services/SSHConnectionManager.swift gains a `setOnProjectDisconnect(_:)` actor method. vmux/Services/TerminalSessionRegistry.swift exposes `handleProjectDisconnect` + `reconnect(tabID:)`. vmux/Views/TerminalWindowView.swift overlays a top-center disconnect banner with Reconnect tap. 6 new tests cover all transitions + project-scoped fanout. xcodebuild build + test pass (32 tests, 1 live SSH skipped).
T-019
T-019 VERIFY: pass — vmux/Views/SkydomeView.swift hosts a RealityView with a custom 48×96-segment inward-facing sphere (radius 30, reversed winding) + UnlitMaterial textured from the PlaceholderPanorama imageset. SkydomeBuilder.makeSkydome falls back to a flipped-scale stock sphere if mesh generation fails. 3 new tests cover topology, radius, and inward-normal correctness. xcodebuild build + test pass (21 tests, 1 live SSH skipped).
T-020
T-020 VERIFY: pass — vmux/Services/PanoramaStore.swift adds a @MainActor @Observable singleton listing PNGs in Documents/panoramas/, atomic save, idempotent delete, setActive(filename:) loads UIImage from disk. SkydomeView observes activeFilename and rebuilds the UnlitMaterial via UIImage → CGImage → TextureResource. 10 new tests. xcodebuild build + test pass (31 passed, 1 live SSH skipped).
T-021
T-021 VERIFY: pass — vmux/Services/OpenAIImageClient.swift adds an actor with generatePanorama(prompt:apiKey:) POSTing to /v1/images/generations with the wrapped prompt + gpt-image-2 + 2048x1024 + b64_json; one-shot model fallback (gpt-image-2 → gpt-image-1 on 404/invalid_model) and size fallback (2048x1024 → 1024x1024 on invalid_size), both surfaced via GenerationResult.warning. StubURLProtocol records (request, body). 4 new tests. xcodebuild build + test pass (36 tests, 1 live SSH skipped).
T-022
T-022 VERIFY: pass — vmux/Views/SettingsView.swift adds a "360 Environment" Section: 32k-capped TextEditor prompt with live counter, Generate button (disabled without OpenAI key, ProgressView during flight), GenerationResult.warning as a yellow banner, OpenAIImageClientError via .alert. On success writes PNG via PanoramaStore.save and setActive so SkydomeView swaps texture live. xcodebuild build + test pass (36 passed, 1 live SSH skipped).
T-023
T-023 VERIFY: pass — vmux/Views/Components/PanoramaPickerGrid.swift adds a LazyVGrid (.adaptive(minimum:120,maximum:160)) over availableFilenames; PanoramaThumbnail loaded via UIImage(contentsOfFile:) with photo-icon fallback, accent-border + checkmark overlay when active. Tap → setActive, long-press context menu → confirmation dialog → delete. Empty state caption. Embedded below the warning banner in SettingsView. xcodebuild build + test pass (36 passed, 1 live SSH skipped).
