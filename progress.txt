T-001
T-001 VERIFY: pass — scaffold backfill committed in 703f691; project.yml + vmux.xcodeproj generated, builds in Xcode/visionOS sim.
T-002
T-002 VERIFY: pass — Package.resolved pins Citadel + SwiftTerm; vmux/SwiftPMImportCheck.swift references SSHClient + Terminal to prove both packages link.
T-003
T-003 VERIFY: pass — `plutil -lint vmux/Info.plist` returned OK; `NSMicrophoneUsageDescription` present with the PRD-specified copy; `NSSpeechRecognitionUsageDescription` correctly absent (Speech framework not used; Gemini Live replaces it).
T-004
T-004 VERIFY: pass — vmux/Models/{Project,Tab,AppSettings}.swift implement the §4.2 schemas; vmuxApp.swift wires ModelContainer + AppSettings.bootstrap; vmuxTests/ModelsTests.swift covers round-trip, cascade delete, and single-row bootstrap.
T-005
T-005 VERIFY: pass — vmuxApp.swift defines all four scenes (sidebar/settings/terminal-per-UUID/environment-immersive-mixed); vmux/Views/{Sidebar,Settings,TerminalWindow,Skydome}View.swift created; SidebarView wires the three temp buttons (Open Settings / Open Terminal / Toggle Environment) via openWindow + openImmersiveSpace per T-005 Do block; `xcodebuild -scheme vmux -destination 'platform=visionOS Simulator,name=Apple Vision Pro' build` and `test` both pass.
T-006
T-006 VERIFY: pass — vmux/Services/KeychainService.swift implements save/load/delete against kSecClassGenericPassword with service "vmux" and account = ref (upsert via SecItemUpdate→Add fallback, idempotent delete); vmuxTests/KeychainServiceTests.swift covers round-trip, overwrite, delete, missing-load-nil, idempotent-delete, and Unicode; `xcodebuild test -scheme vmux -destination 'platform=visionOS Simulator,name=Apple Vision Pro'` passes (10/10 tests).
T-007
T-007 VERIFY: pass — vmux/Views/SidebarView.swift now uses @Query for projects + tabs, single List with "Projects" and "Tabs in <project>" sections, ⚙ Settings button bottom-left wired via openWindow(id:"settings"), tab rows open terminal windows via openWindow(id:"terminal", value: tab.id), context menus for delete project/close tab via modelContext, no-op handlers for "+ New Project"/"+ New Tab" (deferred to T-008/T-012), and the three T-005 temp buttons are gone; frame is 340×640; `xcodebuild build` and `xcodebuild test` both succeed (10/10 tests).
