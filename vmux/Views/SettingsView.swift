import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Settings (placeholder)")
            .font(.title)
            .padding()
            .frame(minWidth: 480, minHeight: 400)
    }
}

#Preview {
    SettingsView()
}
